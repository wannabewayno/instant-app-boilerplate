import React from 'react';
import ReactDOM from 'react-dom';
import './reset.css'; // Apply a css reset
import App from './App.jsx';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);